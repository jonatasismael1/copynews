import test from'node:test';import assert from'node:assert/strict';import{acquireMedia}from'./adapters.mjs'
test('Cobalt rejeita resposta sem mídia',async()=>{const original=global.fetch;global.fetch=async()=>new Response(JSON.stringify({status:'error',error:{code:'blocked'}}),{status:400,headers:{'content-type':'application/json'}});await assert.rejects(()=>acquireMedia('https://example.com',{cobaltUrl:'https://cobalt.test'}),/blocked/);global.fetch=original})
test('normaliza a origem de túnel da Railway',async()=>{const original=global.fetch;global.fetch=async()=>new Response(JSON.stringify({status:'tunnel',url:'https://old-host.up.railway.app/tunnel?id=abc'}),{status:200,headers:{'content-type':'application/json'}});const result=await acquireMedia('https://example.com',{cobaltUrl:'https://current.up.railway.app/'});assert.equal(result.mediaUrl,'https://current.up.railway.app/tunnel?id=abc');global.fetch=original})
