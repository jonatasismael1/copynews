const jsonHeaders={'content-type':'application/json','accept':'application/json'}
export async function acquireMedia(sourceUrl,{cobaltUrl,cobaltKey}){const endpoint=cobaltUrl.replace(/\/$/,'');const response=await fetch(endpoint,{method:'POST',headers:{...jsonHeaders,...(cobaltKey?{'Authorization':`Api-Key ${cobaltKey}`}:{})},body:JSON.stringify({url:sourceUrl,downloadMode:'auto',videoQuality:'1080',filenameStyle:'basic'})});const payload=await response.json().catch(()=>({}));if(!response.ok||payload.status==='error')throw Object.assign(new Error(payload.error?.code||payload.text||`Cobalt HTTP ${response.status}`),{code:'COBALT_ERROR'});let mediaUrl;if(['redirect','tunnel'].includes(payload.status))mediaUrl=payload.url;else if(payload.status==='picker')mediaUrl=payload.picker?.find(x=>x.type==='video')?.url||payload.picker?.[0]?.url;if(!mediaUrl)throw Object.assign(new Error('Cobalt não retornou uma mídia compatível'),{code:'COBALT_EMPTY'});if(payload.status==='tunnel'){const tunnel=new URL(mediaUrl),configured=new URL(endpoint);tunnel.protocol=configured.protocol;tunnel.host=configured.host;mediaUrl=tunnel.toString()}return{mediaUrl,filename:payload.filename||'source.mp4',cobaltStatus:payload.status}}
export async function extractMetadata(sourceUrl){const url=new URL(sourceUrl);try{if(url.hostname.includes('tiktok')){const r=await fetch(`https://www.tiktok.com/oembed?url=${encodeURIComponent(sourceUrl)}`,{signal:AbortSignal.timeout(10_000)});if(r.ok){const x=await r.json();return{caption:x.title||null,author:x.author_name||null,provider:'tiktok-oembed'}}}if(url.hostname.includes('instagram')){const clean=url.origin+url.pathname.replace(/\/$/,'')+'/embed/captioned/';const r=await fetch(clean,{headers:{'User-Agent':'Mozilla/5.0'},signal:AbortSignal.timeout(10_000)});if(r.ok){const html=await r.text();const match=html.match(/"edge_media_to_caption"\s*:\s*\{"edges"\s*:\s*\[\{"node"\s*:\s*\{"text"\s*:\s*"((?:\\.|[^"\\])*)"/);if(match)return{caption:JSON.parse(`"${match[1]}"`),author:null,provider:'instagram-embed'}}}}catch(error){console.warn(JSON.stringify({event:'metadata.failed',message:error.message}))}return{caption:null,author:null,provider:'none'}}
