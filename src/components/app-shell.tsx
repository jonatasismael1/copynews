import{NavLink,Outlet,useLocation}from'react-router-dom';import{BarChart3,FileText,Home,LogOut,Menu,Newspaper,PlusCircle,Settings,Users,X}from'lucide-react';import{useState}from'react';import{Button}from'./ui/button';import{cn}from'@/lib/utils';import{useAuth}from'@/providers/auth-provider'
const items=[['/','Visão geral',Home],['/noticias','Notícias',Newspaper],['/criar','Criar notícia',PlusCircle],['/publicacoes','Publicações',FileText],['/usuarios','Usuários',Users],['/configuracoes','Configurações',Settings]] as const
export function AppShell(){const[open,setOpen]=useState(false);const{profile,signOut}=useAuth();const loc=useLocation();return <div className="min-h-dvh bg-background text-foreground"><aside className={cn('fixed inset-y-0 left-0 z-50 w-72 border-r bg-sidebar p-4 transition-transform lg:translate-x-0',open?'translate-x-0':'-translate-x-full')}><div className="flex h-14 items-center justify-between px-2"><div className="flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-primary text-primary-foreground"><BarChart3 size={20}/></div><div><p className="font-display text-lg font-bold leading-none">Copy News</p><p className="mt-1 text-[10px] uppercase tracking-[.22em] text-muted-foreground">Central editorial</p></div></div><Button className="lg:hidden" variant="ghost" size="icon" onClick={()=>setOpen(false)} aria-label="Fechar menu"><X/></Button></div><nav className="mt-6 space-y-1">{items.filter(([path])=>path!='/usuarios'||profile?.role==='admin').map(([path,label,Icon])=><NavLink key={path} to={path} end={path==='/'} onClick={()=>setOpen(false)} className={({isActive})=>cn('flex h-11 items-center gap-3 rounded-xl px-3 text-sm font-medium transition',isActive?'bg-primary text-primary-foreground shadow-sm':'text-muted-foreground hover:bg-muted hover:text-foreground')}><Icon size={18}/>{label}</NavLink>)}</nav><div className="absolute inset-x-4 bottom-4 rounded-2xl border bg-background/70 p-3"><div className="mb-3 min-w-0"><p className="truncate text-sm font-semibold">{profile?.name}</p><p className="truncate text-xs text-muted-foreground">{profile?.email}</p></div><Button variant="ghost" size="sm" className="w-full justify-start" onClick={signOut}><LogOut/>Sair</Button></div></aside>{open&&<button className="fixed inset-0 z-40 bg-black/30 lg:hidden" onClick={()=>setOpen(false)} aria-label="Fechar menu"/>}<main className="pb-24 lg:ml-72 lg:pb-0"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b bg-background/85 px-4 backdrop-blur-xl sm:px-7"><Button variant="ghost" size="icon" className="lg:hidden" onClick={()=>setOpen(true)} aria-label="Abrir menu"><Menu/></Button><div className="hidden lg:block"><p className="text-xs font-medium uppercase tracking-[.18em] text-muted-foreground">{new Intl.DateTimeFormat('pt-BR',{weekday:'long',day:'2-digit',month:'long'}).format(new Date())}</p></div><div className="flex items-center gap-3"><span className="hidden text-sm text-muted-foreground sm:inline">Olá, <b className="text-foreground">{profile?.name?.split(' ')[0]}</b></span><div className="grid size-9 place-items-center rounded-full bg-secondary text-xs font-bold">{profile?.name?.split(' ').map(x=>x[0]).slice(0,2).join('')}</div></div></header><div key={loc.pathname} className="animate-in p-4 sm:p-7"><Outlet/></div></main><nav className="fixed inset-x-3 bottom-3 z-30 flex h-16 items-center justify-around rounded-2xl border bg-background/95 px-1 shadow-xl backdrop-blur lg:hidden">{items.slice(0,5).filter(([path])=>path!='/usuarios'||profile?.role==='admin').map(([path,label,Icon])=><NavLink key={path} to={path} end={path==='/'} className={({isActive})=>cn('flex min-w-14 flex-col items-center gap-1 rounded-xl py-2 text-[10px] font-semibold',isActive?'text-primary':'text-muted-foreground')}><Icon size={19}/><span>{label.split(' ')[0]}</span></NavLink>)}</nav></div>}
