import{useState}from'react';import{useNavigate}from'react-router-dom';import{ArrowRight,BarChart3,Eye,EyeOff,LockKeyhole,Mail}from'lucide-react';import{useForm}from'react-hook-form';import{zodResolver}from'@hookform/resolvers/zod';import{z}from'zod';import{toast}from'sonner';import{supabase}from'@/lib/supabase';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input'
const schema=z.object({email:z.string().email('Informe um e-mail válido'),password:z.string().min(6,'A senha deve ter ao menos 6 caracteres')});type Values=z.infer<typeof schema>
export function LoginPage(){const[show,setShow]=useState(false);const[forgot,setForgot]=useState(false);const nav=useNavigate();const{register,handleSubmit,formState:{errors,isSubmitting}}=useForm<Values>({resolver:zodResolver(schema)});async function submit(v:Values){if(forgot){const{error}=await supabase.auth.resetPasswordForEmail(v.email,{redirectTo:`${location.origin}/configuracoes`});if(error)toast.error(error.message);else toast.success('Enviamos as instruções para o seu e-mail');return}const{error}=await supabase.auth.signInWithPassword(v);if(error)toast.error('E-mail ou senha inválidos');else nav('/')}return <main className="grid min-h-dvh lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden overflow-hidden bg-sidebar p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-24 -top-24 size-96 rounded-full bg-emerald-400/10 blur-3xl"/><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-xl bg-emerald-400 text-slate-950"><BarChart3/></div><span className="font-display text-xl font-bold">Copy News</span></div><div className="relative max-w-xl"><p className="mb-5 text-xs font-semibold uppercase tracking-[.25em] text-emerald-300">Produção editorial inteligente</p><h1 className="font-display text-5xl font-semibold leading-[1.08] tracking-tight">Da fonte à publicação, sem perder o fio da notícia.</h1><p className="mt-6 max-w-lg text-lg leading-relaxed text-slate-300">Capture, transforme, revise e acompanhe todo o fluxo editorial em um só lugar.</p></div><p className="text-sm text-slate-500">Uso interno • Acesso protegido</p></section><section className="flex items-center justify-center p-6 sm:p-10"><div className="w-full max-w-md"><div className="mb-10 flex items-center gap-3 lg:hidden"><div className="grid size-10 place-items-center rounded-xl bg-primary text-white"><BarChart3 size={20}/></div><span className="font-display text-xl font-bold">Copy News</span></div><p className="text-xs font-semibold uppercase tracking-[.2em] text-primary">Bem-vindo de volta</p><h2 className="mt-3 font-display text-3xl font-bold tracking-tight">{forgot?'Recuperar acesso':'Entre na sua central'}</h2><p className="mt-2 text-muted-foreground">{forgot?'Informe o e-mail cadastrado.':'Use seu e-mail e senha corporativos.'}</p><form className="mt-8 space-y-5" onSubmit={handleSubmit(submit)}><label className="block"><span className="mb-2 block text-sm font-semibold">E-mail</span><div className="relative"><Mail className="absolute left-3 top-3.5 text-muted-foreground" size={17}/><Input className="pl-10" placeholder="voce@empresa.com" autoComplete="email" {...register('email')}/></div>{errors.email&&<span className="mt-1 text-xs text-destructive">{errors.email.message}</span>}</label>{!forgot&&<label className="block"><span className="mb-2 block text-sm font-semibold">Senha</span><div className="relative"><LockKeyhole className="absolute left-3 top-3.5 text-muted-foreground" size={17}/><Input className="px-10" type={show?'text':'password'} autoComplete="current-password" {...register('password')}/><button type="button" className="absolute right-3 top-3 text-muted-foreground" onClick={()=>setShow(!show)} aria-label={show?'Ocultar senha':'Mostrar senha'}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div>{errors.password&&<span className="mt-1 text-xs text-destructive">{errors.password.message}</span>}</label>}<Button className="w-full" size="lg" disabled={isSubmitting}>{isSubmitting?'Aguarde...':forgot?'Enviar instruções':'Entrar'}<ArrowRight/></Button></form><button className="mt-6 w-full text-center text-sm font-semibold text-primary hover:underline" onClick={()=>setForgot(!forgot)}>{forgot?'Voltar para o login':'Esqueci minha senha'}</button><p className="mt-12 text-center text-xs text-muted-foreground">Não possui acesso? Solicite ao administrador.</p></div></section></main>}
