import{useNavigate}from'react-router-dom';import{ArrowLeft,Info,Link2,Sparkles,Video}from'lucide-react';import{useForm}from'react-hook-form';import{zodResolver}from'@hookform/resolvers/zod';import{Button}from'@/components/ui/button';import{Card,CardContent,CardHeader,CardTitle}from'@/components/ui/card';import{Input,Textarea}from'@/components/ui/input';import{createNewsSchema,type CreateNewsInput}from'@/lib/schemas';import{useCreateNews,useLookups}from'@/hooks/use-data'
export function CreateNewsPage(){const nav=useNavigate();const{data}=useLookups();const mutation=useCreateNews();const{register,handleSubmit,formState:{errors}}=useForm<CreateNewsInput>({resolver:zodResolver(createNewsSchema),defaultValues:{editorial_tone:'Jornalístico, claro e direto'}});async function submit(values:CreateNewsInput){const result=await mutation.mutateAsync(values);nav(`/noticias/${result.news_item_id}`)}return <div className="mx-auto max-w-3xl space-y-6"><Button variant="ghost" onClick={()=>nav(-1)}><ArrowLeft/>Voltar</Button><div><p className="text-sm font-semibold text-primary">Nova pauta</p><h1 className="mt-1 font-display text-3xl font-bold">Processar notícia</h1><p className="mt-2 text-sm text-muted-foreground">Cole a publicação original. O processamento continua mesmo se você sair desta tela.</p></div><Card><CardHeader><CardTitle className="flex items-center gap-2"><Link2 className="text-primary"/>Link de origem</CardTitle></CardHeader><CardContent><form className="space-y-6" onSubmit={handleSubmit(submit)}><label className="block"><span className="mb-2 block text-sm font-semibold">URL da publicação *</span><Input placeholder="https://instagram.com/reel/..." inputMode="url" {...register('source_url')}/>{errors.source_url&&<p className="mt-1 text-xs text-destructive">{errors.source_url.message}</p>}<p className="mt-2 flex items-start gap-2 text-xs leading-relaxed text-muted-foreground"><Info size={14} className="mt-0.5 shrink-0"/>Instagram, TikTok, YouTube e outras fontes suportadas pelo Cobalt. Links privados podem não estar disponíveis.</p></label><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold">Responsável</span><select className="h-11 w-full rounded-xl border bg-background px-3 text-sm" {...register('assigned_to')}><option value="">Atribuir depois</option>{data?.profiles.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label><span className="mb-2 block text-sm font-semibold">Categoria</span><select className="h-11 w-full rounded-xl border bg-background px-3 text-sm" {...register('category_id')}><option value="">Sem categoria</option>{data?.categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label><span className="mb-2 block text-sm font-semibold">Página de destino</span><select className="h-11 w-full rounded-xl border bg-background px-3 text-sm" {...register('destination_page_id')}><option value="">Definir depois</option>{data?.pages.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label><span className="mb-2 block text-sm font-semibold">Tom editorial</span><Input {...register('editorial_tone')}/></label></div><label className="block"><span className="mb-2 block text-sm font-semibold">Observações</span><Textarea placeholder="Contexto adicional, atenção a um fato, orientação de créditos..." {...register('notes')}/></label><div className="rounded-2xl border border-primary/15 bg-primary/5 p-4"><div className="flex gap-3"><div className="grid size-9 shrink-0 place-items-center rounded-xl bg-primary/10 text-primary"><Video size={18}/></div><div><p className="text-sm font-semibold">Mídia temporária</p><p className="mt-1 text-xs leading-relaxed text-muted-foreground">O vídeo será usado apenas para preview, download, transcrição e OCR. A limpeza automática remove o arquivo após a expiração.</p></div></div></div><Button className="w-full" size="lg" disabled={mutation.isPending}><Sparkles/>{mutation.isPending?'Enviando...':'Processar notícia'}</Button></form></CardContent></Card></div>}
