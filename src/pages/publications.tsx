import{useState}from'react';import{ExternalLink,Plus,TrendingUp}from'lucide-react';import{useForm}from'react-hook-form';import{zodResolver}from'@hookform/resolvers/zod';import{Button}from'@/components/ui/button';import{Card,CardContent}from'@/components/ui/card';import{Badge}from'@/components/ui/badge';import{Input,Textarea}from'@/components/ui/input';import{useCreatePublication,useLookups,usePublications,useRecordMetrics}from'@/hooks/use-data';import{metricSchema,publicationSchema,type MetricInput,type PublicationInput}from'@/lib/schemas'
export function PublicationsPage(){const{data=[],isLoading}=usePublications();const[modal,setModal]=useState<'publication'|'metrics'|null>(null);const[selected,setSelected]=useState<string>('');return <div className="mx-auto max-w-7xl space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-primary">Distribuição</p><h1 className="mt-1 font-display text-3xl font-bold">Publicações</h1><p className="mt-2 text-sm text-muted-foreground">Registros vinculados e externos, com histórico de métricas.</p></div><Button onClick={()=>setModal('publication')}><Plus/>Adicionar publicação</Button></div><div className="grid gap-3">{isLoading?<p className="text-sm text-muted-foreground">Carregando...</p>:data.length===0?<Card><CardContent className="py-16 text-center"><h2 className="font-display text-lg font-semibold">Nenhuma publicação registrada</h2><p className="mt-2 text-sm text-muted-foreground">Publicações externas também contam no dashboard.</p></CardContent></Card>:data.map(pub=>{const sorted=[...(pub.metric_snapshots??[])].sort((a,b)=>String(b.captured_at).localeCompare(String(a.captured_at)));const last=sorted[0];return <Card key={pub.id}><CardContent className="flex flex-col gap-4 p-4 md:flex-row md:items-center"><div className="min-w-0 flex-1"><div className="flex flex-wrap gap-2"><Badge>{pub.platform}</Badge><Badge variant="outline">{pub.source_type==='external'?'Externa':'Copy News'}</Badge></div><h2 className="mt-3 truncate font-display font-semibold">{pub.title}</h2><p className="mt-1 text-xs text-muted-foreground">{new Intl.DateTimeFormat('pt-BR',{dateStyle:'medium',timeStyle:'short',timeZone:'America/Maceio'}).format(new Date(pub.published_at))} • {pub.pages?.name||'Sem página'}</p></div><div className="grid grid-cols-2 gap-4 text-sm md:flex md:gap-8"><Stat label="Visualizações" value={Number(last?.views??0)}/><Stat label="Interações" value={Number(last?.likes??0)+Number(last?.comments??0)+Number(last?.shares??0)+Number(last?.saves??0)}/></div><div className="flex gap-2"><Button variant="outline" size="sm" onClick={()=>{setSelected(pub.id);setModal('metrics')}}><TrendingUp/>Métricas</Button><Button variant="ghost" size="icon" asChild><a href={pub.published_url} target="_blank" rel="noreferrer" aria-label="Abrir publicação"><ExternalLink/></a></Button></div></CardContent></Card>})}</div>{modal==='publication'&&<PublicationModal close={()=>setModal(null)}/>} {modal==='metrics'&&<MetricsModal publicationId={selected} close={()=>setModal(null)}/>}</div>}
function Stat({label,value}:{label:string;value:number}){return <div><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 font-display text-lg font-bold">{value.toLocaleString('pt-BR')}</p></div>}
function PublicationModal({close}:{close:()=>void}){const{data}=useLookups();const mutation=useCreatePublication();const{register,handleSubmit,formState:{errors}}=useForm<PublicationInput>({resolver:zodResolver(publicationSchema),defaultValues:{published_at:new Date().toISOString().slice(0,16)}});async function submit(v:PublicationInput){await mutation.mutateAsync(v);close()}return <Overlay close={close}><form className="space-y-4" onSubmit={handleSubmit(submit)}><h2 className="font-display text-xl font-bold">Adicionar publicação</h2><p className="text-sm text-muted-foreground">Registre também conteúdos produzidos fora do Copy News.</p><label className="block"><span className="label">Título *</span><Input {...register('title')}/>{errors.title&&<small className="text-destructive">{errors.title.message}</small>}</label><div className="grid gap-3 sm:grid-cols-2"><Field label="Plataforma *"><Input placeholder="Instagram" {...register('platform')}/></Field><Field label="Data e hora *"><Input type="datetime-local" {...register('published_at')}/></Field></div><Field label="URL publicada *"><Input inputMode="url" {...register('published_url')}/></Field><Field label="Página"><select className="h-11 w-full rounded-xl border bg-background px-3 text-sm" {...register('page_id')}><option value="">Sem página</option>{data?.pages.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></Field><Field label="Legenda"><Textarea {...register('caption')}/></Field><Field label="Créditos"><Input {...register('credit_text')}/></Field><div className="grid grid-cols-2 gap-3"><Button type="button" variant="outline" onClick={close}>Cancelar</Button><Button disabled={mutation.isPending}>Registrar</Button></div></form></Overlay>}
function MetricsModal({publicationId,close}:{publicationId:string;close:()=>void}){const mutation=useRecordMetrics();const{register,handleSubmit}=useForm<MetricInput>({resolver:zodResolver(metricSchema),defaultValues:{publication_id:publicationId,captured_at:new Date().toISOString().slice(0,16),views:0,reach:0,impressions:0,likes:0,comments:0,shares:0,saves:0,clicks:0,followers_gained:0}});async function submit(v:MetricInput){await mutation.mutateAsync(v);close()}return <Overlay close={close}><form className="space-y-4" onSubmit={handleSubmit(submit)}><h2 className="font-display text-xl font-bold">Novo snapshot</h2><p className="text-sm text-muted-foreground">Os valores anteriores serão preservados.</p><Field label="Data da coleta"><Input type="datetime-local" {...register('captured_at')}/></Field><div className="grid grid-cols-2 gap-3">{(['views','reach','impressions','likes','comments','shares','saves','clicks','followers_gained'] as const).map(name=><Field key={name} label={{views:'Visualizações',reach:'Alcance',impressions:'Impressões',likes:'Curtidas',comments:'Comentários',shares:'Compartilhamentos',saves:'Salvamentos',clicks:'Cliques',followers_gained:'Seguidores ganhos'}[name]}><Input type="number" min="0" {...register(name,{valueAsNumber:true})}/></Field>)}</div><div className="grid grid-cols-2 gap-3"><Button type="button" variant="outline" onClick={close}>Cancelar</Button><Button disabled={mutation.isPending}>Salvar snapshot</Button></div></form></Overlay>}
function Overlay({children,close}:{children:React.ReactNode;close:()=>void}){return <div className="fixed inset-0 z-50 flex items-end bg-black/40 sm:items-center sm:justify-center" onClick={close}><Card className="max-h-[90dvh] w-full overflow-y-auto rounded-b-none p-5 sm:max-w-xl sm:rounded-2xl" onClick={e=>e.stopPropagation()}>{children}</Card></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block"><span className="mb-2 block text-sm font-semibold">{label}</span>{children}</label>}
